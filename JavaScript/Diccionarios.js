// Definir el objeto "coche"
let coche = {
    marca: 'Chana',
    color: 'blanco',
    modelo: 'sedan',
    placa: 'DAD190'
};

// Imprimir el color actual
console.log(coche.color);

// Cambiar el color del coche
coche.color = 'Negro';
console.log(coche.color);

// Imprimir la marca actual
console.log(coche.marca);

// Cambiar la marca del coche
coche.marca = 'Renault';
console.log(coche.marca);

// Imprimir el objeto completo
console.log(coche);
